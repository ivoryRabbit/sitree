// SPA mode: no server-side rendering. The page boots on the client and fetches
// its data from the backend at runtime, so there is nothing to prerender.
export const ssr = false;
export const prerender = false;
